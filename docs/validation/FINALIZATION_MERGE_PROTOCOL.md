# FINALIZATION MERGE PROTOCOL (Elite Docs)

1. Run gates: pytest, ruff, mypy, evidence audit, DOI validation, final diff validation, evidence enforcer.
2. Generate/update sign-off (`MERGE_SIGN_OFF_TEMPLATE.txt`) with 4-lead approvals.
3. Ensure working tree clean; commit only validated artifacts.
4. Open PR with gate outputs and checklist completion.
5. Merge after approvals; tag release if policy requires.
6. Start maintenance log update (`docs/maintenance/2026_q2_calibration.md`).
